export class Tag {

    constructor(
        {
            id,
            name,
            zipball_url,
            tarball_url
        }
    ) {
        this.id = id
        this.name = name
        this.zipball_url = zipball_url
        this.tarball_url = tarball_url
    }
}