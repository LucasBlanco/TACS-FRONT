export class User {

    constructor(
        {
            id,
            username,
            admin
        }
    ) {
        this.id = id
        this.username = username
        this.admin = admin
    }

}
